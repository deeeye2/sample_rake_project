require 'rake'

desc "Default task"
task :default do
  puts "Running the default Rake task..."
end

desc "Say hello"
task :say_hello do
  puts "Hello from Rake!"
end

desc "Run the main script"
task :run_main do
  ruby "src/main.rb"
end
